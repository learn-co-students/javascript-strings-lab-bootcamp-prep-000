var greeting = "Hello, everybody!";
//stores the string hello everybody in the var "greeting"

var specialGuest = "Neil deGrasse Tyson"
//does the same thing as above

var greetSpecialGuest = "Hello, " + specialGuest + "!";
//stores a string plus a previous var into a new var

var topic = "space";

var conversation = `Let\'s talk about ${topic}.`;
//escapes the ' with a backslash, interpolates the variable inside a string so that it runs
